// import { pokemonInfo } from '../../services/pokemonServices';
// import { useGetPokemon } from '../../services/pokemonServices';

// const Buscador = () => { 
//   const { pokemones } = useGetPokemon()

//   return (
//     <form>
//     {pokemones.map((pokemon) => (
//       <div key={pokemon.id}> {/* Assuming pokemons have unique ids */}
//         <input type="text" placeholder="Encuentra a tu pokemon" />
//         <button type="submit" onClick={() => pokemonInfo(pokemon.name)}>Buscar</button>
//       </div>
//     ))}
//   </form>
//   )
// }

// export default Buscador;